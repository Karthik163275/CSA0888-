def plusOne(digits):
    n=len(digits)-1
    while digits[n]==9:
        digits[n]=0
        n=n-1
    if n<0:
        digits=[1]+digits
    else:
        digits[n]=digits[n]+1
    return digits
digits=[1,2,5,7]
print(plusOne(digits))
 4 changes: 4 additions & 0 deletions4  
E14.SQUARE ROOT.py
@@ -0,0 +1,4 @@
import math
number=16
square_root=math.sqrt(number)
print(square_root)
